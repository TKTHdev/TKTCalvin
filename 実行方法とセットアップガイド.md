# CalvinDB 実行方法とセットアップガイド

## 概要

CalvinDBは、決定論を活用してアクティブレプリケーションと完全なACID準拠の分散トランザクションを二相コミットなしで保証するスケーラブルなトランザクションデータベースシステムです。このコードベースは、オリジナルのCalvinDBと低レイテンシー版のCalvinDBの両方をサポートしています。

## システム要件

### 推奨環境
- **OS**: Ubuntu 14.04以降（他のLinuxディストリビューションでも類似の手順）
- **現在のテスト環境**: Debian GNU/Linux 12 (bookworm)
- **コンパイラ**: g++ 12.2.0以降

### 必要なソフトウェア

#### 基本ツール
```bash
# システムパッケージマネージャーで以下をインストール
sudo apt-get update
sudo apt-get install -y make g++ autoconf libtool libreadline-dev subversion git unzip tar cmake libsnappy-dev pkg-config
```

#### 外部ライブラリ（自動インストールスクリプト付き）
- **protobuf** - プロトコルバッファライブラリ
- **glog** - Googleのログライブラリ  
- **zeromq** - 高性能非同期メッセージングライブラリ
- **gflags** - コマンドラインフラグライブラリ

## インストール手順

### ステップ 1: 依存関係のインストール

プロジェクトには自動化されたインストールスクリプト `install-ext` が提供されています：

```bash
cd ~/CalvinDB
./install-ext
```

このスクリプトは以下を自動で実行します：
- 必要なシステムパッケージのインストール
- 外部ライブラリ（gflags、glog、protobuf、zeromq）のダウンロードとコンパイル
- 最新のコンパイラとの互換性確保

### ステップ 2: 環境変数の設定

コンパイルした外部ライブラリへのパスを設定：

```bash
# ~/.bashrc に以下を追加
export LD_LIBRARY_PATH=~/CalvinDB/ext/zeromq/src/.libs:~/CalvinDB/ext/protobuf/src/.libs:~/CalvinDB/ext/glog/.libs:~/CalvinDB/ext/gflags/.libs

# 設定を反映
source ~/.bashrc
```

### ステップ 3: CalvinDBのコンパイル

```bash
cd ~/CalvinDB/src
make -j
```

- `-j` オプションは並列コンパイルを有効にし、ビルド時間を短縮します
- コンパイルされたバイナリは `~/CalvinDB/bin/` に生成されます

## 設定ファイル

### クラスター設定 (calvin.conf)

クラスター内のすべてのマシンを含むよう設定ファイルを編集：

```
# 形式: マシンID:レプリカID:IPアドレス:ポート
0:0:192.168.122.1:10001
1:0:192.168.122.2:10001
2:1:192.168.122.3:10001
3:1:192.168.122.4:10001
4:2:192.168.122.5:10001
5:2:192.168.122.6:10001
```

**設定項目の説明：**
- **第1列**: グローバルマシンID
- **第2列**: レプリカID
- **第3列**: IPアドレス
- **第4列**: ポート番号

上記の例では3つのレプリカがあり、各レプリカには2台のマシンが含まれています。

### ローカル開発用設定例

```
0:0:127.0.0.1:10001
1:1:127.0.0.1:10002
2:2:127.0.0.1:10003
```

## 実行方法

### クラスター管理コマンド

CalvinDBは `cluster` コマンドを通じて管理されます：

```bash
cd ~/CalvinDB
```

#### 基本的な管理操作

```bash
# クラスターの状態確認
bin/scripts/cluster --command="status" --type=1

# 最新コードの取得とコンパイル
bin/scripts/cluster --command="update" --type=1

# 設定ファイルをクラスターの全マシンに送信
bin/scripts/cluster --command="put-config" --type=1
```

### CalvinDBの起動

#### オリジナル CalvinDB

**3レプリカ構成で起動：**
```bash
bin/scripts/cluster --command="start" --lowlatency=0 --type=0 --experiment=0 --percent_mp=0 --percent_mr=0 --hot_records=10000 --max_batch_size=100
```

**6レプリカ構成で起動：**
```bash
bin/scripts/cluster --command="start" --lowlatency=0 --type=1 --experiment=0 --percent_mp=0 --percent_mr=0 --hot_records=10000 --max_batch_size=100
```

#### 低レイテンシー CalvinDB

**3レプリカ構成で起動：**
```bash
bin/scripts/cluster --command="start" --lowlatency=1 --type=0 --experiment=0 --percent_mp=0 --percent_mr=0 --hot_records=10000 --max_batch_size=100
```

**6レプリカ構成で起動：**
```bash
bin/scripts/cluster --command="start" --lowlatency=1 --type=1 --experiment=0 --percent_mp=0 --percent_mr=0 --hot_records=10000 --max_batch_size=100
```

**6レプリカ構成（強可用性）で起動：**
```bash
bin/scripts/cluster --command="start" --lowlatency=1 --type=2 --experiment=0 --percent_mp=0 --percent_mr=0 --hot_records=10000 --max_batch_size=100
```

### パラメータ説明

| パラメータ | 説明 |
|-----------|------|
| `--lowlatency` | 0: オリジナルCalvinDB, 1: 低レイテンシー版CalvinDB, 2: アクセスパターンリマスター付き低レイテンシー版 |
| `--type` | CalvinDB: 0=3レプリカ, 1=6レプリカ / 低レイテンシー: 0=3レプリカ通常, 1=6レプリカ通常, 2=6レプリカ強可用性 |
| `--experiment` | 実行する実験タイプ（デフォルト: マイクロベンチマーク） |
| `--percent_mp` | 分散トランザクションの割合 |
| `--percent_mr` | マルチレプリカトランザクションの割合 |
| `--hot_records` | ホットレコード数（競合制御用） |
| `--max_batch_size` | エポックごとのトランザクション最大バッチサイズ |

### プロセスの停止

テスト完了後、別のターミナルでデータベースプロセスを停止：

```bash
# オリジナルCalvinDBの停止（6レプリカ）
bin/scripts/cluster --command="kill" --lowlatency=0 --type=1

# 低レイテンシーCalvinDBの停止（6レプリカ）
bin/scripts/cluster --command="kill" --lowlatency=1 --type=1

# 3レプリカ構成の停止
bin/scripts/cluster --command="kill" --lowlatency=0 --type=0
```

## プロジェクト構造

```
CalvinDB/
├── README                    # 基本的な説明文書
├── calvin.conf              # クラスター設定ファイル
├── install-ext              # 依存関係自動インストールスクリプト
├── src/                     # ソースコード
│   ├── Makefile            # メインMakefile
│   ├── applications/       # アプリケーション層（TPCC、マイクロベンチマーク）
│   ├── backend/            # ストレージバックエンド
│   ├── common/             # 共通ユーティリティ
│   ├── log/                # ログシステム（Paxos実装含む）
│   ├── machine/            # クラスター管理、接続管理
│   ├── proto/              # プロトコルバッファ定義
│   ├── scheduler/          # 決定論的スケジューラー
│   └── scripts/            # 実行スクリプト
├── bin/                    # コンパイル済みバイナリ（自動生成）
├── ext/                    # 外部ライブラリ（自動生成）
└── logs/                   # ログファイル（自動生成）
```

## トラブルシューティング

### コンパイルエラー

1. **外部ライブラリの問題**
   ```bash
   # ライブラリを再インストール
   rm -rf ext/
   ./install-ext
   ```

2. **環境変数の問題**
   ```bash
   # 環境変数を確認
   echo $LD_LIBRARY_PATH
   # 正しく設定されていない場合は再設定
   source ~/.bashrc
   ```

3. **Makefileの問題**
   ```bash
   # クリーンビルド
   cd src/
   make clean
   make -j
   ```

### 実行時エラー

1. **ポートが使用中**
   - calvin.confで別のポートを指定
   - または使用中のプロセスを停止

2. **ネットワーク接続の問題**
   - IPアドレスとポートの設定を確認
   - ファイアウォール設定を確認

### SSH接続（分散環境）

複数マシンでの実行時は、各データセンター用のSSHキーを設定：

```bash
# cluster.ccで設定されるSSHキーパス例
~/.ssh/id_rsa  # 各データセンター用のキー
```

## 高度な設定

### カスタムビルド設定

`src/Makefile` で最適化レベルを変更可能：

```makefile
# 本番環境用（最適化）
OPT_MODE := $(PROD)    # -O2 -DNDEBUG

# デバッグ用
OPT_MODE := $(DEBUG)   # -g2

# プロファイリング用
OPT_MODE := $(PROFILING) # -O2 -g2 -DNDEBUG
```

### 実験設定

- `--experiment=0`: マイクロベンチマーク
- `--experiment=1`: TPCC（TPC-Cベンチマーク）

### パフォーマンスチューニング

- `--hot_records`: 値を下げると競合が増加
- `--max_batch_size`: バッチサイズを大きくするとスループット向上
- `--percent_mp`/`--percent_mr`: 分散/マルチレプリカトランザクションの比率調整

## 参考情報

### 学術論文・技術資料
- CalvinDBの詳細な技術仕様については、関連する学術論文を参照
- 決定論的データベースシステムの理論的背景

### 関連プロジェクト
- CalvinFS: CalvinDBに基づく分散ファイルシステム
- FaunaDB: 商用CalvinDBベースのデータベース

---

**注意**: このプロジェクトは開発中のGeo-replicated CalvinDBコードベースです。本番環境での使用前には十分なテストを実施してください。